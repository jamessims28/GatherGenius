import "./globals.css";

export const metadata = {
  title: "GatherGenius Elite Business Model",
  description: "Elite premium automated event marketplace platform"
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
