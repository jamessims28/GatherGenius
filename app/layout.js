import "./globals.css";

export const metadata = {
  title: "GatherGenius Soft White Automation",
  description: "Soft white premium automated event marketplace platform"
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
