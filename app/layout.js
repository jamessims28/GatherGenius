import "./globals.css";

export const metadata = {
  title: "GatherGenius | Payment System",
  description: "Premium event payment and split tracking app",
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
